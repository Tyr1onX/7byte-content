# Shared Motion Components

这里存放跨 episode 复用的 Motion Canvas 组件和视觉 token。

只有被至少两期视频真正需要的东西才提升到 `shared/`；单期实验先留在 episode 内，避免过早抽象。

未来候选：BrowserFrame、TerminalFrame、NetworkNode、Packet、ProtocolBadge、7BYTE outro。
