# EP009 Script — U 盘真的不能直接拔吗？

直接拔 U 盘，不一定会坏。真正危险的是：它还在写，你就拔了。

“安全弹出”就是让系统把没完成的写入收尾，再停止使用这个设备。

从 Windows 10 1809 开始，外部存储默认用“快速删除”，Windows 不再默认缓存这类磁盘写入，所以设备空闲时，直接拔的风险比以前低。

但正在复制文件，或者你改成了“更好的性能”，就先弹出。

所以最稳妥：不确定，就点安全弹出。

这里是 7BYTE，把计算机讲简单一点。
