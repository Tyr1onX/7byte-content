# Storyboard v1

## 00:00–00:05 — 现实入口
浏览器/程序化浏览器 UI 中输入 `baidu.com`，停 0.5 秒，回车。
主信息：一个普通动作即将展开。

## 00:05–00:09 — 问题
画面简化，只保留 `baidu.com`。
文字：`浏览器首先要知道：百度在哪？`
主运动：无，给阅读时间。

## 00:09–00:16 — DNS
电脑固定左侧；DNS 出现在右上。
一个明确的 request packet 用约 1.1 秒过去，停顿，再由 response packet 返回。
返回结果：`IP address`。

## 00:16–00:23 — 建立通信
DNS 降低存在感，电脑位置不变；服务器出现在右侧。
连接线从电脑画向服务器。
文字先出现 `建立连接`，随后出现锁与 `加密通信`。

## 00:23–00:30 — HTTP request
沿既有连接发送一个实体 packet：`GET /`。
不使用发光球。

## 00:30–00:41 — 资源返回
服务器按节奏返回 `HTML → CSS → JS → IMG`。
左侧浏览器/页面骨架随着返回内容逐步完善，建立因果。

## 00:41–00:47 — 回到结果
抽象浏览器与真实/仿真网页结果对齐 crossfade。
文字：`于是，你看到了网页。`

## 00:47–00:51 — 片尾
深色背景，7BYTE + `把计算机讲简单一点。`
不做粒子爆炸或炫技转场。
