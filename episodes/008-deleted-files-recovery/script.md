# EP008 Script — 文件都删了，为什么还能恢复？

文件都删了，为什么还能恢复？

因为删除通常不是立刻擦掉数据，而是把原来的空间标成“可以再用”。

新数据覆盖前，旧内容可能还在，恢复软件就是在找这些残留。

机械硬盘上尤其常见。

SSD 更难恢复，因为系统通常还会发 `TRIM`，告诉 SSD 这些块不用了，可以被回收。

记住：删除不等于安全擦除。误删后想恢复，先停止写入。

这里是 7BYTE，把计算机讲简单一点。
