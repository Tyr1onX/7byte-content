# EP005 Script — 同样是 1GB，为什么硬盘厂商和 Windows 算得不一样？

500GB 硬盘变 465GB，为什么单位不统一？

其实标准早就统一了：GB 是 10 亿字节，GiB 才是 1024 的三次方字节。

Windows 为什么还常写 GB？早期计算机按 2 的幂算，1024 接近 1000，这些名字就一直沿用。

1998 年，IEC 引入 KiB、MiB、GiB，正式把两套算法分开。

但软件习惯没有一起改，Windows Explorer 至今仍沿用旧标签。

所以没统一的不是定义，而是显示习惯。GB 是 10 的 9 次方，GiB 是 2 的 30 次方。

下次看到 500GB 变 465，先看看两边是不是用了同一把尺子。

这里是 7BYTE，把计算机讲简单一点。

## 口播策略

- Voice：`zh-CN-YunyangNeural`
- Rate：`-5%`
- 不额外加快 TTS；通过压缩重复铺垫，而不是牺牲可消化语速。
- V1 实测 `1998 IEC`：`27.92s`。
- V2 实测 `1998 IEC`：`26.03s`，仍晚于本期 `20–24s` 实验窗口。
- V3 进一步压缩 Hook 与历史铺垫，目标让 `1998 IEC` 落入约 `22–24s`，后半解释与结论保持不变。
